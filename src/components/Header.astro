---
import { base } from '../config';

const navItems = [
  { label: 'Home', href: base },
  { label: 'Politics', href: `${base}/category/politics` },
  { label: 'Economy', href: `${base}/category/economy` },
  { label: 'Employment', href: `${base}/category/employment` },
  { label: 'Education', href: `${base}/category/education` },
  { label: 'About', href: `${base}/about` },
];

const currentPath = Astro.url.pathname;
---

<header class="bg-white shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Masthead -->
    <div class="text-center py-8">
      <h1 class="newspaper-header">
        <a
          href={base}
          class="text-gray-900 no-underline hover:text-canadian-red transition-colors"
        >
          stopbleeding.ca
        </a>
      </h1>
      <p class="text-sm text-gray-600 mt-2 font-sans uppercase tracking-wider">
        Canadian News Analysis
      </p>
    </div>

    <!-- Navigation -->
    <nav class="py-4">
      <ul
        class="flex flex-wrap justify-center gap-4 md:gap-8 font-sans text-sm font-semibold uppercase tracking-wide"
      >
        {
          navItems.map((item) => (
            <li>
              <a
                href={item.href}
                class={`
                hover:text-canadian-red transition-colors
                ${currentPath === item.href ? 'text-canadian-red border-b-2 border-canadian-red' : 'text-gray-700'}
              `}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
